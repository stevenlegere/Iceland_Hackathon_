import React from 'react';
import '../index.css';

const NavBar: React.FC = () => {
    return (
        <nav className="navbar">
            <h1>Smoothie Selector</h1>
        </nav>
    );
};

export default NavBar;